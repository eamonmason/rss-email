# Example environment variables for RSS Email system
# Copy this to .env and update with your actual values

# Claude AI Configuration (optional - for enhanced email categorization)
CLAUDE_ENABLED=true
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here
CLAUDE_MODEL=claude-3-5-haiku-20241022

# Alternative: Use AWS Parameter Store for API key (production recommended)
# ANTHROPIC_API_KEY_PARAMETER=rss-email-anthropic-api-key

# AWS Configuration (usually set via AWS CLI or instance role)
# AWS_DEFAULT_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Email Configuration (for production deployment)
# SOURCE_DOMAIN=yourdomain.com
# SOURCE_EMAIL_ADDRESS=newsletter@yourdomain.com
# TO_EMAIL_ADDRESS=recipient@example.com
# EMAIL_RECIPIENTS=user1@example.com,user2@example.com

# Functional Test Configuration (for test_functional_workflow.py)
# Enable fast mode to limit articles for quicker Claude testing
FAST_MODE=true
# Maximum number of articles to process in fast mode (default: 15)
MAX_TEST_ARTICLES=10